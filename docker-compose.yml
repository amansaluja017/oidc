services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: oidc
      POSTGRES_PASSWORD: oidc
      POSTGRES_DB: oidc_auth
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: